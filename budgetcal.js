function budgetCalculator(watch,phone,laptop);
{
    var watchCost = 50;
    total_watch_Cost = watch * watchCost;

   var phoneCost = 100;
   total_Phone_Cost = phone * phoneCost;

   var laptopCost = 500;
   total_laptop_Cost = laptop * laptopCost;

   var totalBudget = total_Watch_Cost + total_Phone_Cost + total_laptop_Cost;

   return totalBudget;
}
